# README

## Проект
Одностраничный портфолио-сайт в файле `index2.html` с поддержкой двух языков: арабский (основной, RTL) и английский (второй, LTR). Дизайн: тёмный градиентный фон, минималистичный стиль, блоки Hero / About / Contacts.

## Текущий статус
- Статус: `in progress`
- Последнее обновление: 2026-02-17
- Ответственный: Codex + пользователь

## План работ
1. Сформировать базовую HTML-структуру страницы.
2. Реализовать стили и адаптивность (desktop + mobile).
3. Добавить содержимое секций и контактные ссылки.
4. Локализовать интерфейс на арабский язык и включить RTL.
5. Добавить второй язык (английский) и переключатель языков.
6. Проверить итоговую структуру и актуализировать документацию.

## TODO
- [x] Создать страницу портфолио в отдельном файле `index2.html`.
- [x] Добавить секции: Hero, About, Contacts.
- [x] Добавить mailto и ссылки на соцсети со встроенными SVG-иконками.
- [x] Перевести страницу на арабский язык.
- [x] Установить `lang="ar"` и `dir="rtl"`.
- [x] Добавить английский как второй язык сайта.
- [x] Добавить переключатель языка AR/EN с обновлением `lang` и `dir`.
- [ ] Заменить placeholder-ссылки на реальные профили (`GitHub`, `LinkedIn`, `Telegram`).
- [ ] При необходимости заменить демонстрационный текст "О себе"/"About Me" на финальный.

## Чат-бот Никифор
- На сайте подключён чат-бот **Никифор** (виджет в правом нижнем углу). Основа: [evgyur/cursor-ai-chatbot](https://github.com/evgyur/cursor-ai-chatbot).
- Никифор отвечает на вопросы о владельце сайта и подсказывает, как связаться (email, соцсети).
- **Чтобы бот работал на Vercel:** в [Vercel Dashboard](https://vercel.com) → проект **portfolio-site** → Settings → Environment Variables добавь переменную **`MINIMAX_API_KEY`** (ключ из [MiniMax Platform](https://platform.minimax.io)).
- Локально: скопируй `.env.example` в `.env` и укажи свой `MINIMAX_API_KEY` (для локального теста API нужен свой backend или прокси).
- Файлы: `api/chat.js` (Vercel serverless), `nikifor-widget.js` (виджет), подключение в `index.html` и `index2.html`.

## Деплой (Vercel)
- **Продакшен:** https://portfolio-site-seven-sand-53.vercel.app
- Ветка для деплоя: `master`. По `git push origin master` можно деплоить вручную через Vercel CLI (`vercel --prod`) или включить автодеплой (см. ниже).
- **Автодеплой при push:** в [Vercel Dashboard](https://vercel.com) → проект **portfolio-site** → Settings → Git: подключи репозиторий GitHub (Login Connection), затем выбери Production Branch = `master`. После этого каждый `git push origin master` будет автоматически собирать и выкатывать сайт.

## Структура проекта
- `index.html` — исходный/предыдущий вариант страницы.
- `index2.html` — актуальная версия страницы (AR + EN).
- `readme.md` — рабочая документация проекта (этот файл).
- `vercel.json` — конфиг Vercel (статический сайт + API).
- `api/chat.js` — serverless-функция для чат-бота (MiniMax).
- `nikifor-widget.js` — виджет чата «Никифор» на странице.
- `.env.example` — пример переменных для бота (MINIMAX_API_KEY).
- `mcps/` — служебная директория окружения.

## Документация по странице (`index2.html`)
- Формат: один файл (HTML + встроенный CSS + встроенный JS для i18n).
- Семантика: `<header>`, `<section>`, `<footer>`.
- Адаптивность: `meta viewport` + media query для мобильных экранов.
- Стили: тёмный фон, фиолетовые акценты, hover-эффекты, стеклянные панели.
- Контакты: email через `mailto:` и 3 социальных ссылки.
- Языки: переключение AR/EN кнопками в Hero-блоке.
- Локализация: смена текстов, `document.title`, `html lang`, `html dir`.

## Правила ведения README
Перед завершением любой значимой задачи в этом проекте обязательно:
1. Обновить `Текущий статус`.
2. Актуализировать блок `TODO` (отмечать выполненные пункты и добавлять новые).
3. Обновить `Структура проекта`, если появились/изменились файлы.
4. Кратко зафиксировать, что именно изменилось в документации по странице.

## Журнал изменений
### 2026-02-17
- Создан `readme.md` как единая рабочая документация проекта.
- Зафиксированы план, to-do, структура и правила обязательного обновления README перед завершением задач.
- Добавлена поддержка второго языка (English) в `index2.html` и переключатель AR/EN.
- Подтверждено обновление README после задачи по двуязычности сайта.
